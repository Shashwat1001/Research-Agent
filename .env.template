OPENAI_API_KEY=<your api key>
SERPAPI_KEY=<your serpapi key>
# Optional features
RERANK_SERP=1         # 1 = re-rank search results via embeddings before fetching
RERANK_CHUNKS=1       # 1 = re-rank extracted chunks via embeddings after BM25
SEARCH_ENGINES=serpapi,tavily  # comma-separated: serpapi, tavily

# Tavily (optional but recommended if you enable it above)
TAVILY_API_KEY=
